{
  "name": "tinqa-studio-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd apps/studio-web dev",
    "build": "yarn --cwd apps/studio-web build"
  }
}